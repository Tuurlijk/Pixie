defaults:
  username: vagrant
  host: local.typo3.org
  root: /var/www

sites:
  localhost:
    root: ~/Projects/TYPO3/Development
  homestead:
    username: vagrant
    host: local.typo3.org
    root: /var/www
    directory:
    database:
      platform: mysql
      username: typo3
      password: supersecret
      admin_username: root
      admin_password: supersecret
  example:
    username: www-data
    host: some.dev.pixie.io
    root: /var/www
    directory: some.dev.pixie.io
    database:
      platform: mysql
      host: localhost
      username: pixie_usr
      password: &F&nxqL27WTk&P[My2%m8b96yG#6#/W4
      database: pixie_dev
